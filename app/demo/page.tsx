"use client";
import { SiteNav } from "@/components/site/SiteNav";
import { RuntimeVisual } from "@/components/site/VisualSystem";
import { DecisionEngine } from "@/components/site/DecisionEngine";
import { Reveal } from "@/components/site/Reveal";

export default function Demo(){return <main className="site-page"><SiteNav/><section className="page-hero"><div className="eyebrow">INTERACTIVE DEMO / 11</div><h1>See the decision<br/><em>before the outcome.</em></h1><p>Issue an agent action. Change the scenario. Watch the governance layer evaluate identity, policy and context before execution.</p></section><section className="demo-stage"><Reveal><RuntimeVisual mode="policy"/></Reveal><Reveal><div className="visual-copy"><div className="eyebrow">RUNTIME SIMULATION</div><h2>Three actions.<br/><em>Three decisions.</em></h2><p>Move between representative runtime requests and see the resulting allow, block or human-approval path.</p></div></Reveal></section><Reveal><DecisionEngine/></Reveal><section className="dark-section split"><Reveal><div><div className="eyebrow">THE AUDIT MOMENT</div><h2>Every decision can become an event.</h2></div></Reveal><Reveal><p>The production system can connect a governed decision to its agent, policy, context, outcome and audit trail.</p></Reveal></section></main>}